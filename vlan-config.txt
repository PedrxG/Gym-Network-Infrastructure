!
! ==========================================================
!  GymLink VLAN Configuration File
!  Author: Pedro Gaetjens Molongua
!  Device: Core/Distribution Switch
!  Purpose: VLAN Initialization and Naming
! ==========================================================
!
! Configure VLAN 10 (Management) - High privilege access
Switch(config)# vlan 10
Switch(config-vlan)# name Management
Switch(config-vlan)# exit
!
! Configure VLAN 20 (Staff) - Internal operational use
Switch(config)# vlan 20
Switch(config-vlan)# name Staff
Switch(config-vlan)# exit
!
! Configure VLAN 30 (Customer Service) - Client-facing operations
Switch(config)# vlan 30
Switch(config-vlan)# name CustomerService
Switch(config-vlan)# exit
!
! Configure VLAN 40 (IT) - Network maintenance and monitoring
Switch(config)# vlan 40
Switch(config-vlan)# name IT
Switch(config-vlan)# exit
!
! Configure VLAN 50 (Guest) - Isolated Wi-Fi access (Internet Only)
Switch(config)# vlan 50
Switch(config-vlan)# name Guest
Switch(config-vlan)# exit
!
! --- END VLAN CONFIGURATION ---
!
! Verification Command:
! The following command should be executed after applying the configuration 
! to verify the successful creation and naming of all five VLANs.
!
! Switch# show vlan brief
!
